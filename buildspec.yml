version: 0.2
phases:
  install:
    runtime-versions:
      dotnet: latest
    commands:
      - echo Installing Entity Framework Core tools reference - .NET CLI
      - dotnet tool install --global dotnet-ef

  pre_build:
    commands:
      - echo Restore started on `date`
      - dotnet restore Lvmendes.Adm.ApiAdm/Lvmendes.Adm.ApiAdm.csproj
  build:
    commands:
      - echo Build started on `date`      
      - dotnet build Lvmendes.Adm.ApiAdm/Lvmendes.Adm.ApiAdm.csproj

     
  post_build:
    commands:
      - echo Publish started on `date`
      # https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/dotnet-linux-platform-bundle-app.html
      - export BUCKET=recreioquentinha #or use a different bucket other than your codepipeline bucket
      - dotnet publish -c release -o ./build_output Lvmendes.Adm.ApiAdm/Lvmendes.Adm.ApiAdm.csproj
      - aws cloudformation package --template-file ./build_output/template.yml --s3-bucket recreioquentinha --output-template-file ./build_output/outputtemplate.yml --debug
      

artifacts:
  files:
        - ./build_output/template.yml
        - ./build_output/outputtemplate.yml
 

